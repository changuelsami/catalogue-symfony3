{% extends 'base.html.twig' %}

{% block body %}
    <h1>Produits list</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>Description</th>
                <th>Prix HT</th>
                <th>Catégorie</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for produit in produits %}
            <tr>
                <td><a href="{{ path('eshop_admin_produit_show', { 'id': produit.id }) }}">{{ produit.id }}</a></td>
                <td>{{ produit.libelle }}</td>
                <td>{{ produit.description }}</td>
                <td>{{ produit.prixHT }}</td>
                <td>{{ produit.categorie.libelle }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('eshop_admin_produit_show', { 'id': produit.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('eshop_admin_produit_edit', { 'id': produit.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('eshop_admin_produit_new') }}">Create a new produit</a>
        </li>
    </ul>
{% endblock %}
